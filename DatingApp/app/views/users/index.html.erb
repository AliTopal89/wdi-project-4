<h2> Find someone </h2>
<p>
<%= link_to "Limit To Nearby Users", users_path(nearby: 20) %>

</p>

Users:
<div id='conversation'>
<% @users.each do |user| %>
	<li><b>
		<%= user.username %> 
		<%=link_to "Start Conversation", new_message_path(recipient_id: user) %>
		<%=button_to "Like", likes_path(likeable_id: user.id) %>

	</li>
<% end %>
</div>
